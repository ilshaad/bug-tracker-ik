{
  "name": "backend-bug-tracker",
  "version": "1.0.0",
  "description": "backend for the bug tracker",
  "main": "server.js",
  "scripts": {
    "server": "nodemon src/server.js",
    "ikcleanci": "rm -fr node_modules/ && npm cache clean -vf && npm ci && npm run server",
    "ikupdate": "npm i && npm update && npm audit fix --force && npm outdated"
  },
  "author": "Rechad Salma",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.16"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dompurify": "^2.3.6",
    "dotenv": "^16.0.0",
    "express": "^4.18.1",
    "jsdom": "^19.0.0",
    "morgan": "^1.10.0",
    "pg": "^8.7.3"
  }
}
